#include <ostream>;

// functions and variables should not share the same identifier pool

void foo() {
    cout << "this be a test function" << "\n";
}

i32 main() {

    i32 foo = 10;
    cout << "FOO : " << foo << "\n";
    foo();

    return 0;
}