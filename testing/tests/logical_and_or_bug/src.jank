#include <iostream>;
#include <random>;
#include <assert>;

i32 main() {

    u64 bits = 0b0101;
    u64 flag = 0b0100;
    cout << (1 < 2) << "\n";
    cout << $i32 (bits & flag) << "\n";
    if(1 < 2 && $i32 (bits & flag)) {
        cout << "this should print\n";
    }

    return 0;
}