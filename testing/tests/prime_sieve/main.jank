#include <jstring>;
#include <put>;

i32 main() {
    i32 n = 100;
    for(i32 i = 2; i <= n; i = i + 1) {
        i32 is_prime = 1;
        for(i32 j = 2; j < i; j = j + 1) {
            if(i % j == 0) is_prime = 0;
        }
        if(is_prime) puti_endl(i);
    }
    return 0;
}