
i64 main() {
    i64 n = 100;
    for(i64 i = 2; i <= n; i = i + 1) {
        i64 is_prime = 1;
        for(i64 j = 2; j < i; j = j + 1) {
            if(i % j == 0) is_prime = 0;
        }
        if(is_prime) puti_endl(i);
    }
    return 0;
}