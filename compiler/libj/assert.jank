#include <put>;

template<T> 
void assert(T x, u8* msg) {
    if(!x) {
        puts("assert failed : ");
        puts_endl(msg);
        sys_exit(1);
    }
}

template<T>
void assert(T x, string msg) {
    assert(x, msg.jstr());
}

template<T>
void assert(T x) {
    assert(x, "no msg");
}

