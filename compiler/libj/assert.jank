#include <put>;

void assert(i32 x) {
    if(!x) {
        puts_endl("assert failed");
        sys_exit(1);
    }
}

void assert(u64 x) {
    if(!x) {
        puts_endl("assert failed");
        sys_exit(1);
    }
}