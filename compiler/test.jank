#include <iostream>;
#include <assert>;
#include <wait>;
#include <exec>;

i32 main() {

    // i32 ret = execp("jjc");
    // cout << "EXEC FAILED : " << ret << "\n";

    dirstream* dir = opendir(".");
    cout << "DIR : " << dir << "\n";
    
    return 0;
}