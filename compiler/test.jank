#include <iostream>;
#include <assert>;
#include <wait>;
#include <exec>;

#include "test2.jank";

struct A {
    B foo;
}

void test() {
    A foo = new A();
    foo.foo.foo.foo.foo = 10;
    cout << "FOO : " << foo.foo.foo.foo.foo << "\n";
}

i32 main() {

    test();
    cout << "this should compile\n";
    
    return 0;
}