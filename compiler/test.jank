#include <put>;
#include <jstring>;
#include <vector>;
#include <iostream>;
#include <assert>;

i32 main() {

    {
        u32 a = $u32 10;
        u32 b = $u32 100;
        cout << (b / a) << "\n";
        b /= $u32 3;
        cout << b << "\n";
        cout << (a * b) << "\n";
    }
    
    {
        u16 a = $u16 10;
        u16 b = $u16 100;
        cout << (b / a) << "\n";
        b /= $u16 3;
        cout << b << "\n";
        cout << (a * b) << "\n";
    }

    return 0;
}