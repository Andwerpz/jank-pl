#include <bits/stdc++.h>
using namespace std;

/*
this is a tester to check the correctness of parse_generator.cpp when parsing and generating ebnf parsers. 
*/

/*
first, generate an ebnf parser from ebnf.ebnf

things per testcase to check 
 - make sure amount of rules that parse_generator parses equals expected (>= amount of rules that should successfully parse)
 - make sure semantic checks pass / fail. (maybe can also check the reason?)
 - make sure amount of rules ebnf parser parses equals expected
*/

signed main() {

    

    return 0;
}