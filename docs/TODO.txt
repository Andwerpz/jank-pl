
(main quest)
Add IR (D:)
- this will make codegen easier, the instructions will map more directly to assembly. 
- this will make performance improvements to generated code easier


(main quest)
Make test runner support multi-file tests
- gains ability to test linking multiple generated assembly files
- gains ability to test import semantics


(side quest)
Add compiler flag where you can specify default imports
- One usecase is enabling compiler mode and replacing the default <malloc> with a kernel malloc.
- also should be able to specify files from stdlib as default


(side quest)
Make parsing faster
- alot of the parsing will happen when we're trying to just gather function definitions. 
  we can probably target this to make faster by writing a grammar that only sees function definitions and skips everything else. 
- look into a tokenizer
- first, do benchmarking to figure out where the current parser is slow!!


(side quest)
Add better semantic error messages to the compiler
- with the introduction of SourceSpans, we can probably print much better error messages to the user 
- probably have to clean up the code alot in order to make this nice to do. 
- if we get this working, should add a 'semantic error' mode to the compiler and add semantic error highlighting to 
  the highlighting extension. 
  - maybe even can implement ctrl+click!!


(side quest)
Rewrite jjc in jank
- mainly for the operating system, and for the flex
- probably want to wait until IR is done before doing this, so the compiler can actually be relatively fast (relatively).


(side quest)
Make test runner multithreaded
- rework so that each test compiles to a separate directory before running. 
- have a test job queue that threads take from 
- notify all threads to stop when first test fails. 
- maybe also have a nice test duration display like bazel does
- this will hopefully make running tests much faster


(side quest)
Make jank build tool
- its name is 'dylan'
- should be something like cargo from rust
