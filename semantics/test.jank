
//assignment update, sum query segment tree
struct segtree {
    int n;
    int* a;

    segtree segtree() {}
    segtree segtree(int n) {
        
    }

}

//just a vector<int>
struct vector {
    int sz;
    int cap;
    int* a;

    vector vector() {
        this.sz = 0;
        this.cap = 1;
        this.a = malloc(sizeof(int) * 1);
    }

    vector vector(int n) {
        this.sz = n;
        this.cap = n;
        this.a = malloc(sizeof(int) * 1);
        for(int i = 0; i < n; i = i + 1) this.a[i] = 0;
    }

    void pop_back(int x) {

    }

    void push_back(int x) {

    }
}

int main() {
    
    int n = 1000;
    vector a = vector(n);
    segtree segt = segtree(n);

    return 0;
}